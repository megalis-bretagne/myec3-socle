/**
 * Provides business object model classes (POJO).
 * 
 * Classes of this package provide the business model but also define, with annotations, the persistence mapping (JPA
 * javax.persistence annotations), the XML serialization characteristics (JAXB javax.xml annotations), the bean
 * validation attributes (Bean Validation javax.validation annotations)
 */
package org.myec3.socle.core.domain.model;

